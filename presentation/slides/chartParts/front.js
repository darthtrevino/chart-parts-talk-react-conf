import React from 'react'
import notes from '../../notes'
import TwoStepHeader from '../../components/TwoStepHeader'

export default (
	<TwoStepHeader
		notes={notes.whatif}
		key="showcase_what_if"
		text="What if we had Vega's abstraction level"
		sub="...in React?"
	/>
)
